caution: for window only

run development
```bash
docker-compose-v1.exe -f .\docker-compose.dev.yml up --build
```

run production
```bash
docker-compose-v1.exe -f .\docker-compose.prod.yml up --build
```

todo: for unix distros